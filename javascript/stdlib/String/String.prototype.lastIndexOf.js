import { demo, log, warn, err, dir, line, typeis } from "../util.js";

/*  String.prototype.lastIndexOf(subStr[,fromIndex])
@param {String} subStr
@param {Number} fromIndex 初始偏移量
@return {Number} 序号，-1表示没找到
语义：子串最后一次匹配到时的偏移量(索引值)
特性：
（同indexOf接口）
*/
